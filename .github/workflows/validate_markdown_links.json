{
    "ignorePatterns": [
        {
            "description": "Ignore anchor links that currently cannot be resolved. See https://github.com/tcort/markdown-link-check/issues/225",
            "pattern": "^(?!http).*#.+"
        },
        {
            "description": "This link initially returns a 403 to prevent access to bots. Ignoring this to prevent the workflow from failing.",
            "pattern": "https://valentina-db.com/en/all-downloads/vstudio"
        }
    ],
    "aliveStatusCodes": [200, 203, 206]
}
